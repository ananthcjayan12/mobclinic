meta {
  name: Get Payment Summary
  type: http
  seq: 5
}

get {
  url: {{base_url}}/api/method/mob_clinic.mob_clinic.api.payment.get_payment_summary?patient_id={{patient_id}}
  body: none
  auth: none
}

query {
  patient_id: {{patient_id}}
}
