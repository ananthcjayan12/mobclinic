meta {
  name: Get Appointment
  type: http
  seq: 3
}

get {
  url: {{base_url}}/api/method/mob_clinic.mob_clinic.api.appointment.get_appointment?appointment_id={{appointment_id}}
  body: none
  auth: none
}

query {
  appointment_id: {{appointment_id}}
}
